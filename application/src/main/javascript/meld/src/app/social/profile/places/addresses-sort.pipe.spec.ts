import { AddressesSortPipe } from './addresses-sort.pipe';

describe('AddressesSortPipe', () => {
  it('create an instance', () => {
    const pipe = new AddressesSortPipe();
    expect(pipe).toBeTruthy();
  });
});
