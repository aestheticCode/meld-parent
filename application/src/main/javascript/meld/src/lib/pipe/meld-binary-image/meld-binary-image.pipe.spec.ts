import { MeldBinaryImagePipe } from './meld-binary-image.pipe';

describe('MeldBinaryImagePipe', () => {
  it('create an instance', () => {
    const pipe = new MeldBinaryImagePipe();
    expect(pipe).toBeTruthy();
  });
});
