import { MeldTbodyDirective } from './meld-tbody.directive';

describe('MeldTbodyDirective', () => {
  it('should create an instance', () => {
    const directive = new MeldTbodyDirective();
    expect(directive).toBeTruthy();
  });
});
