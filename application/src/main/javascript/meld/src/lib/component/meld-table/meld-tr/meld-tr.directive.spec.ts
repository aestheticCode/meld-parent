import { MeldTrDirective } from './meld-tr.directive';

describe('MeldTrDirective', () => {
  it('should create an instance', () => {
    const directive = new MeldTrDirective();
    expect(directive).toBeTruthy();
  });
});
