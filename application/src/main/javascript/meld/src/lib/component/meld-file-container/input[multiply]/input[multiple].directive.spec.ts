import { InputMultiplyDirective } from './input[multiple].directive';

describe('InputDirective', () => {
  it('should create an instance', () => {
    const directive = new InputMultiplyDirective(null);
    expect(directive).toBeTruthy();
  });
});
