import { MeldFilterDirective } from './meld-filter.directive';

describe('MeldFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new MeldFilterDirective();
    expect(directive).toBeTruthy();
  });
});
