import { MeldTfooterDirective } from './meld-tfooter.directive';

describe('MeldTfooterDirective', () => {
  it('should create an instance', () => {
    const directive = new MeldTfooterDirective();
    expect(directive).toBeTruthy();
  });
});
