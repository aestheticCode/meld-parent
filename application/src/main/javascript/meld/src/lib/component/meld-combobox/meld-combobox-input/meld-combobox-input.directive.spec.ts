import { MeldComboboxInputDirective } from './meld-combobox-input.directive';

describe('MeldComboboxInputDirective', () => {
  it('should create an instance', () => {
    const directive = new MeldComboboxInputDirective(null);
    expect(directive).toBeTruthy();
  });
});
