import { MeldTdDirective } from './meld-td.directive';

describe('MeldTdDirective', () => {
  it('should create an instance', () => {
    const directive = new MeldTdDirective();
    expect(directive).toBeTruthy();
  });
});
