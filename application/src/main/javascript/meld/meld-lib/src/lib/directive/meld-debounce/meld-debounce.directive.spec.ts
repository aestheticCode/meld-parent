import { MeldDebounceDirective } from './meld-debounce.directive';

describe('MeldDebounceDirective', () => {
  it('should create an instance', () => {
    const directive = new MeldDebounceDirective(null);
    expect(directive).toBeTruthy();
  });
});
