import { MeldTouchWheelDirective } from './meld-touch-wheel.directive';

describe('MeldTouchWheelDirective', () => {
  it('should create an instance', () => {
    const directive = new MeldTouchWheelDirective(null);
    expect(directive).toBeTruthy();
  });
});
