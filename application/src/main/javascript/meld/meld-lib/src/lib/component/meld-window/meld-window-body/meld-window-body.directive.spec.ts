import { MeldWindowBodyDirective } from './meld-window-body.directive';

describe('MeldWindowBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new MeldWindowBodyDirective();
    expect(directive).toBeTruthy();
  });
});
