import { MeldColgroupDirective } from './meld-colgroup.directive';

describe('MeldColgroupDirective', () => {
  it('should create an instance', () => {
    const directive = new MeldColgroupDirective();
    expect(directive).toBeTruthy();
  });
});
