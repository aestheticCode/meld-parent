import { MeldPlaceholderPipe } from './meld-placeholder.pipe';

describe('MeldPlaceholderPipe', () => {
  it('create an instance', () => {
    const pipe = new MeldPlaceholderPipe();
    expect(pipe).toBeTruthy();
  });
});
