import { MeldColDirective } from './meld-col.directive';

describe('MeldColDirective', () => {
  it('should create an instance', () => {
    const directive = new MeldColDirective();
    expect(directive).toBeTruthy();
  });
});
