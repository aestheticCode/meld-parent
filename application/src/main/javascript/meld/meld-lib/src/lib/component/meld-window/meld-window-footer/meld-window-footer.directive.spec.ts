import { MeldWindowFooterDirective } from './meld-window-footer.directive';

describe('MeldWindowFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new MeldWindowFooterDirective();
    expect(directive).toBeTruthy();
  });
});
