import { MeldTableSorterPipe } from './meld-table-sorter.pipe';

describe('MeldTableSorterPipe', () => {
  it('create an instance', () => {
    const pipe = new MeldTableSorterPipe();
    expect(pipe).toBeTruthy();
  });
});
