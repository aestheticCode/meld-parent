import { MeldWindowHeaderDirective } from './meld-window-header.directive';

describe('MeldWindowHeadDirective', () => {
  it('should create an instance', () => {
    const directive = new MeldWindowHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
