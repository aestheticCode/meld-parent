import { MeldControlValuePipe } from './meld-control-value.pipe';

describe('MeldControlValuePipe', () => {
  it('create an instance', () => {
    const pipe = new MeldControlValuePipe();
    expect(pipe).toBeTruthy();
  });
});
